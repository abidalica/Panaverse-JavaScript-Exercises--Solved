// start writing your code here. When you are done, try saving it into assignment31.js file.

let userList = ["eric","abdul","scott","admin","tiger"];


userList = [];

if (userList.length == 0) {
    console.log("We need to find some users.");
} else {
    console.log(`User list is not empty, there are ${userList.length} users in the list.`);
}